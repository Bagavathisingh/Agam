# Comprehensive struct field modification test

கட்டமைப்பு Point:
    x
    y

கட்டமைப்பு Person:
    name
    age
    city

அச்சிடு("Test 1: Basic struct modification")
மாறி p = Point(10, 20)
அச்சிடு("  Before: p.x =", p.x, ", p.y =", p.y)
p.x = 100
p.y = 200
அச்சிடு("  After: p.x =", p.x, ", p.y =", p.y)

அச்சிடு("")
அச்சிடு("Test 2: String field modification")
மாறி person = Person("அருண்", 25, "சென்னை")
அச்சிடு("  Before:", person.name, person.age, person.city)
person.age = 30
person.city = "கோயம்புத்தூர்"
அச்சிடு("  After:", person.name, person.age, person.city)

அச்சிடு("")
அச்சிடு("✅ All struct field modification tests passed!")
